import styled from 'styled-components'

export const TravelsWrapper = styled.main`
  display: flex;
  flex-direction: column;
  padding: 2.5em 1em;
  gap: 2.25em;
  align-items: center;

  @media (min-width: 500px) {
    padding: 2.5em 2.8125em;
  }
`
